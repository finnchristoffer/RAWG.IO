name: DocC

on:
  push:
    branches: [main]
    paths:
      - '**/*.swift'
      - '**/*.md'
      - '**/*.docc/**'
  pull_request:
    branches: [main]
    paths:
      - '**/*.swift'
      - '**/*.md'
      - '**/*.docc/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: docc-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build Documentation
    runs-on: macos-15
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.2'
      
      - name: Prepare Xcode
        run: |
          sudo xcodebuild -license accept
          sudo xcodebuild -runFirstLaunch
      
      - name: Install iOS Simulator Runtime
        run: |
          echo "Downloading iOS platform..."
          xcodebuild -downloadPlatform iOS
          
          echo "Available devices:"
          xcrun simctl list devices available
      
      - name: Install Mise
        uses: jdx/mise-action@v2
        with:
          install: true
      
      - name: Fetch Dependencies
        run: tuist install
      
      - name: Generate Project
        run: tuist generate --no-open
      
      - name: Build Project First
        run: |
          tuist build RAWGApp -- \
            -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' \
            -derivedDataPath DerivedData \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO
      
      - name: Build Documentation
        run: |
          xcodebuild docbuild \
            -workspace RAWG.xcworkspace \
            -scheme RAWGApp \
            -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' \
            -derivedDataPath DerivedData \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO
      
      - name: Process Documentation
        run: |
          DOCC_ARCHIVE=$(find DerivedData -name "*.doccarchive" -type d | head -1)
          
          if [ -z "$DOCC_ARCHIVE" ]; then
            echo "No documentation archive found"
            exit 1
          fi
          
          echo "Found: $DOCC_ARCHIVE"
          
          $(xcrun --find docc) process-archive \
            transform-for-static-hosting "$DOCC_ARCHIVE" \
            --hosting-base-path RAWG.IO \
            --output-path docs
          
          DOC_FOLDER=$(ls docs/documentation/ | head -1)
          echo "<meta http-equiv=\"refresh\" content=\"0; url=documentation/$DOC_FOLDER/\">" > docs/index.html
      
      - name: Upload Pages Artifact
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4